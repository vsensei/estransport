export type TransitType = 'WALK' | 'TRAM' | 'BUS' | 'RAIL';

/* Use for realtime transport data
export type Location = {
  type: `${TransportType}`;
  lineNumber: string;
  lat: number;
  lon: number;
  headingDegrees: number;
  endStopName: string;
  speed?: string;
};

export const enum TransportType {
  TROLLEYBUS = 1,
  BUS = 2,
  TRAM = 3,
  NIGHTBUS = 7,
}
*/
